# Vitis build trees: ~100 MB per DUT, fully regenerated by run.tcl.
f32_proj/
f32_wide_proj/
f32_pad_proj/
f64_proj/
ab_proj/
i32_proj/
u32_proj/
fixed_proj/
fix24_proj/
logs/
# results/ is NOT ignored -- the C-sim and co-sim outputs are the evidence this package exists
# to produce, and they are small text files.
