# Vitis build trees: ~290 MB across the three DUTs, fully regenerated by run.tcl.
gemv_f32_top_proj/
gemv_ab_top_proj/
gemv_fixed_top_proj/
logs/
# results/ is NOT ignored -- the C-sim and co-sim outputs are the evidence this package exists
# to produce, and they are small text files.
