#ifndef WAVEFLOW_GEN_STATE_ACCUM_PORTS_H
#define WAVEFLOW_GEN_STATE_ACCUM_PORTS_H
// state_accum_ports.h -- GENERATED by waveflow (build/composite_gen.py::render_ports_h) from the SAME
// TopSpec that emits state_accum's interface pragmas.  DO NOT EDIT: regenerate instead.
// One spec, two consumers -- so a testbench cannot drift from the kernel it drives.

namespace state_accum_ports {

static const char* const TOP        = "state_accum";
static const char* const DESIGN_DLL = "xsim.dir/state_accum/xsimk.dll";

static const char* const s_in     = "s_in";   // axis_in
static const char* const m_out    = "m_out";   // axis_out

// Every TB-driven input the interface models above do not themselves drive.  Absent names
// are skipped at bind time (XsiSim::pin_low), so this may name channels a given kernel
// does not expose.
static const char* const ZERO_PORTS[] = {
};
static const int ZERO_PORTS_N = (int)(sizeof(ZERO_PORTS)/sizeof(ZERO_PORTS[0]));

}  // namespace state_accum_ports

#endif  // WAVEFLOW_GEN_STATE_ACCUM_PORTS_H
