# Build products — regenerated by `python -m examples.vecmult.vecmult_build`.
gen/
include/
logs/
vec_mult_proj/

# results/ is deliberately NOT ignored: results/sweep.json is the measured corpus that
# docs/guide/resource_model/example.md quotes, and results/resources.json is the attributed
# report for the DEFAULT design point.  Both are committed so the docs can be checked against
# something rather than trusted.
