// rf_relayout.cpp — GENERATED by waveflow (build/composite_gen.py::render_top).
// DO NOT EDIT: regenerate instead.  Free-running (ap_ctrl_none) hls::task top, derived from
// the component/interface graph — ports, pragmas, channels, one task per active child.
// The task BODIES instantiated below are separate artifacts and are not generated here.
// Verify via XSI — ap_ctrl_none Vitis cosim is unreliable.
#include "hls_task.h"
#include "hls_stream.h"
#include <ap_int.h>
#include "memmgr.hpp"
#include "rf_relayout_to_dense_task.h"
#include "rf_relayout_to_slots_task.h"

void rf_relayout(
    hls::stream<ap_uint<64> >& s_in,
    hls::stream<ap_uint<64> >& s_out
) {
#pragma HLS INTERFACE axis port=s_in
#pragma HLS INTERFACE axis port=s_out
#pragma HLS INTERFACE ap_ctrl_none port=return
    hls_thread_local hls::stream<ap_uint<64> > dense;
    hls_thread_local hls::task t0(rf_relayout_to_dense_task<64, 4, 2>, s_in, dense);
    hls_thread_local hls::task t1(rf_relayout_to_slots_task<64, 4, 2>, dense, s_out);
}
